class Class {
  constructor(params = {}) {
    this.name = params.name || "Biology Honours";
    this.abbrev1 = params.abbrev1 || "Bio H";
    this.abbrev2 = param.abbrev2 || "bio";
    this.periods = select([], params);
    this.teacher = params.teacher || 
  }
}

let School = {
  classList: {},

};

function generateSchool() {

}
